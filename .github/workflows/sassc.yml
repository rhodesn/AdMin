name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Install dependencies
      run: apt update && apt install -y sassc
    - name: Run a multi-line script
      run: echo testing
